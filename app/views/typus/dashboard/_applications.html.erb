<% typus_user.applications.each do |app| %>

  <table>

    <thead>
      <tr>
        <th><%= Typus::I18n.t(app) %></th>
      </tr>
    </thead>

    <% typus_user.application(app).each do |model| %>

      <%
        klass = model.typus_constantize
        klass_human_name = klass.model_name.human.pluralize
        typus_items_path = { :controller => "/typus/#{klass.to_resource}" }
      %>

      <tr class="<%= cycle("even", "odd") %>">
        <td>
          <%= link_to klass_human_name, typus_items_path %>
        </td>
      </tr>

    <% end %>

  </table>

<% end %>
